/** Stub for vite-plugin-manus-runtime when not installed (e.g. local dev). Replace with real package on Manus. */
export function vitePluginManusRuntime() {
  return { name: "vite-plugin-manus-runtime" };
}
